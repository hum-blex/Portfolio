import { Newsreader, PT_Sans,Antic_Didone} from "next/font/google";
export const sans = PT_Sans({
    subsets: ['latin'],
    weight: ['400', '400'],
  });

  export const antic = Antic_Didone({
    subsets: ['latin'],
    weight: ['400', '400'],
  });

  export const newsreader = Newsreader({
    subsets: ['latin'],
    weight: ['400', '400'],
  });